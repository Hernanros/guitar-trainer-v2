# server/scripts — standalone diagnostic / admin scripts.
# This package marker lets scripts be imported as `from scripts.eval_drills import run_eval`
# from the server/ working directory, and also executed as `python -m scripts.eval_drills`.
