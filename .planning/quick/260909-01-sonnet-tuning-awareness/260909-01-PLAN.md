---
phase: quick-260909-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/ai/breakdown.py
  - mobile/src/components/TabNotation.tsx
autonomous: true
requirements:
  - TASK-16
must_haves:
  truths:
    - "Sonnet SYSTEM_PROMPT contains an explicit TUNING section listing the 6 common alt-tunings (Open E, Open D, Open G, DADGAD, Drop D, Drop C) with example songs"
    - "Sonnet is mandated to emit tab.tuning reflecting its choice, and to include a 'Tuning: <name>' technique note whenever the chosen tuning is non-standard"
    - "The _TOOL_DEF description on line 71 no longer instructs Sonnet to default to standard tuning (redundant with the new prompt section)"
    - "TabNotation renders a compact 'Tuning: <label>' line ABOVE the SVG tab grid when tab.tuning != ['E','A','D','G','B','e']"
    - "TabNotation shows the friendly name for the 6 well-known tunings (Open E, Open D, Open G, DADGAD, Drop D, Drop C) and falls back to the raw joined array for anything else"
    - "TabNotation renders nothing extra (no label) when tuning is standard, null, or undefined — defensive"
    - "Server-side Python compiles cleanly and mobile-side tsc --noEmit reports no new errors"
  artifacts:
    - path: "server/app/ai/breakdown.py"
      provides: "Updated SYSTEM_PROMPT with TUNING section + sharpened _TOOL_DEF description"
      contains: "TUNING (CRITICAL"
    - path: "mobile/src/components/TabNotation.tsx"
      provides: "Tuning label above tab grid + common-tuning lookup table"
      contains: "Tuning:"
  key_links:
    - from: "server/app/ai/breakdown.py (SYSTEM_PROMPT)"
      to: "Sonnet 4.6 breakdown output"
      via: "system= param in client.messages.create()"
      pattern: "system=SYSTEM_PROMPT"
    - from: "mobile/src/components/TabNotation.tsx"
      to: "tab.tuning prop (from Breakdown.tab.tuning)"
      via: "Tab type import from api/generated/schema"
      pattern: "tab.tuning"
---

<objective>
Ship Task #16: make Sonnet tuning-aware for songs in non-standard tunings, and surface tuning on mobile when non-standard.

Purpose: The Lenny/SRV incident (investigation 260908) proved Sonnet silently defaults to standard EADGBE and translates the arrangement, destroying trust for intermediate/advanced players who know the song's real tuning. Two-pronged fix: prompt-level (Sonnet identifies canonical tuning and labels it) + UI-level (mobile surfaces the tuning label so the user sees Sonnet's choice).

Output: Updated breakdown SYSTEM_PROMPT + sharpened tool description on the server; TabNotation renders a "Tuning: <name>" label when non-standard on mobile. Single atomic commit under Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/investigations/260908-sonnet-tuning-quality.md
@.planning/design/fletcher-identity.md
@CLAUDE.md
@mobile/CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From server/app/models/song.py — Tab schema (NO change; already permits any 6-string tuning):
```python
class Tab(BaseModel):
    measures: list[Measure]
    tuning: list[str] = ["E", "A", "D", "G", "B", "e"]  # standard low-to-high
```

Current SYSTEM_PROMPT structure in server/app/ai/breakdown.py (lines 36-55) — the TUNING block will be INSERTED between the existing bullet list (ending line 46 "Do not exceed 8 measures.") and the "CRITICAL RULES:" section starting line 48. Keep the "unless otherwise specified" phrasing intact on line 39 — the new section makes it non-conservative in practice while preserving the escape hatch.

Current _TOOL_DEF description (server/app/ai/breakdown.py:70-73):
```
"Emit a full technique breakdown for the requested song. "
"Include a semantically accurate tab (4-8 measures, standard tuning unless the song calls for otherwise), "
"chord diagrams for every chord referenced in the tab, and 3-5 technique notes "
"focusing on the target skills the user is working on."
```
Sharpen to reference the new SYSTEM_PROMPT tuning block; remove the "standard tuning unless the song calls for otherwise" phrase.

From mobile/src/components/TabNotation.tsx — component signature and existing pattern:
```typescript
export function TabNotation({ tab }: { tab: Tab }) {
  const tuning = tab.tuning ?? ['E', 'A', 'D', 'G', 'B', 'e'];
  // ... renders <View style={styles.wrapper}><ScrollView>...<Svg>...</Svg></ScrollView></View>
}
```
The tuning label is added as a sibling <Text> INSIDE styles.wrapper, ABOVE the <ScrollView>. Do not alter the SVG core rendering.

Typography tokens established in mobile/src/app/breakdown/[songId].tsx MetaLine pattern (styles.meta on line 308):
- fontSize: 13, color: '#666' for meta text
- color '#999' for muted section labels
- color '#E07B39' for accent
Use fontSize 12, color '#999' for the "Tuning:" label prefix, and color '#E07B39' fontWeight '600' for the tuning name value — matches the SongOfDayCard/MetaLine visual language.

Fletcher voice contract for the new "Tuning: <name>" technique note (from .planning/design/fletcher-identity.md):
- Diagnostic + coaching pattern: sharp identification, then concrete next step
- Second person, direct + warm, no fake enthusiasm
- Example landing: "Tune your A, D, and G strings UP by a whole step; leave low E, B, and high E alone. Your D becomes E, A becomes B, G becomes G#."
The investigation writeup already provides this exact phrasing — use it verbatim as the example inside the prompt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tuning-aware TUNING section to Sonnet SYSTEM_PROMPT + sharpen tool description</name>
  <files>server/app/ai/breakdown.py</files>
  <action>
    Update SYSTEM_PROMPT (currently lines 36-55) to insert a new "TUNING (CRITICAL — DO NOT SKIP):" block between the bullet list (line 46 "Do not exceed 8 measures.") and the "CRITICAL RULES:" section (line 48). Use the recommended prompt text verbatim from .planning/investigations/260908-sonnet-tuning-quality.md lines 63-84 — this is the LOCKED design, do not paraphrase or restructure.

    The inserted block MUST include:
    - The instruction to identify the song's canonical tuning before writing tab
    - The 6 alt-tunings with their arrays and example songs (Open E, Open D, Open G, DADGAD, Drop D, Drop C) exactly as listed in the investigation
    - The mandate to emit tab.tuning reflecting the choice
    - The mandate to include a "Tuning: <name>" technique note when non-standard, with the verbatim Open E example ("Tune your A, D, and G strings UP by a whole step; leave low E, B, and high E alone. Your D becomes E, A becomes B, G becomes G#.")
    - The "prefer authentic over simplified translations" line and the simplification-labeling escape hatch

    Keep line 39 ("standard notation (E A D G B e tuning unless otherwise specified)") UNCHANGED — the new TUNING section works with the existing bullet, not against it.

    Also update _TOOL_DEF description (lines 70-73). Replace the current phrase "standard tuning unless the song calls for otherwise" with a sharper reference that points at the new prompt block. Suggested rewrite for the description string:
    "Emit a full technique breakdown for the requested song. Include a semantically accurate tab (4-8 measures, in the song's canonical tuning per the TUNING protocol in the system prompt), chord diagrams for every chord referenced in the tab, and 3-5 technique notes focusing on the target skills the user is working on."

    Do NOT change the Tab schema, the Breakdown model, the @governed decorator, retry logic, or any other behavior in this file. Prompt-only change.

    The "Tuning: <name>" technique note MUST fit Fletcher's voice (per .planning/design/fletcher-identity.md): direct, second-person, diagnostic + coaching. The investigation's example phrasing already satisfies this — do not embellish.
  </action>
  <verify>
    <automated>python3 -m py_compile server/app/ai/breakdown.py && grep -c "TUNING (CRITICAL" server/app/ai/breakdown.py | grep -q "^1$" && grep -c "Open E:" server/app/ai/breakdown.py | grep -q "^1$" && grep -c "DADGAD" server/app/ai/breakdown.py | grep -q "^1$" && grep -c "canonical tuning" server/app/ai/breakdown.py | grep -q "^1$" && ! grep -q "standard tuning unless the song calls for otherwise" server/app/ai/breakdown.py</automated>
  </verify>
  <done>SYSTEM_PROMPT contains the TUNING block with all 6 alt-tunings and the mandate to emit tab.tuning + a "Tuning: <name>" technique note when non-standard; _TOOL_DEF description no longer contains the "standard tuning unless the song calls for otherwise" phrase and instead references the TUNING protocol; py_compile passes; no schema or logic changes elsewhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Render tuning label above tab grid in TabNotation when non-standard</name>
  <files>mobile/src/components/TabNotation.tsx</files>
  <action>
    Add a compact "Tuning: <label>" line rendered ABOVE the existing horizontal ScrollView + Svg inside styles.wrapper. Do NOT modify the SVG rendering, Measure component, layout constants, or scroll behavior — the tuning label is a wrapper concern only.

    Implementation steps:

    1. Define a module-level constant STANDARD_TUNING = ['E', 'A', 'D', 'G', 'B', 'e'] (matches the schema default from server/app/models/song.py).

    2. Define a module-level KNOWN_TUNINGS lookup that maps a serialized tuning array (join with '|') to a friendly display name. Include all 6 tunings from the investigation writeup (must match the arrays in the SYSTEM_PROMPT exactly):
       - 'E|A|D|G|B|e' is the standard case (do NOT show a label — see step 4)
       - 'E|B|E|G#|B|E' -> 'Open E'
       - 'D|A|D|F#|A|D' -> 'Open D'
       - 'D|G|D|G|B|D' -> 'Open G'
       - 'D|A|D|G|A|D' -> 'DADGAD'
       - 'D|A|D|G|B|E' -> 'Drop D'
       - 'C|G|C|F|A|D' -> 'Drop C'

    3. Add a helper (module-scope or inline) that takes tab.tuning and returns either the friendly name (via KNOWN_TUNINGS) or the raw array joined with a single space (e.g. "E B E G# B E"). When both are known, render the label in the form: "Tuning: Open E — E B E G# B E". When the tuning is not in KNOWN_TUNINGS, render "Tuning: <space-joined array>".

    4. In the component body, compute a boolean isNonStandard by comparing tab.tuning to STANDARD_TUNING (element-wise). Defensive fallback: if tab.tuning is null, undefined, or does not have length 6, treat as standard (render no label). If tab.tuning is standard, render no label.

    5. When isNonStandard is true, render a <Text> node ABOVE the <ScrollView> inside the existing <View style={styles.wrapper}>. Style:
       - Prefix "Tuning: " uses fontSize 12, color '#999'
       - Tuning value (friendly name or raw joined array) uses color '#E07B39', fontWeight '600'
       - Use nested <Text> to allow the two-color effect within a single line
       - Add styles.tuningLabel with marginBottom 6 so the label breathes above the SVG
       - If rendering the "friendly name — raw array" form, the raw-array suffix uses color '#999' (not the accent) so the friendly name reads as the primary token

    6. Do NOT change styles.wrapper, styles.scrollContent, or the Svg tree. Add new styles (tuningLabel, tuningValue, tuningRaw or similar) to the existing StyleSheet.create block. No new external imports beyond what is already used (react-native Text is already imported via ScrollView/View — verify and add Text to the import if not already present; note the file currently imports only ScrollView, StyleSheet, View from react-native, so Text MUST be added to that import line).

    7. Preserve type-safety: the existing `type Tab = components['schemas']['Tab']` covers tuning: string[] | undefined per the OpenAPI schema. No new type declarations needed.

    Do NOT add tests (no test infrastructure exists for TabNotation in this codebase; the investigation writeup's mention of a "prop test" is out of scope for this quick task per constraints).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -v "^$" | grep -c "src/components/TabNotation.tsx" | grep -q "^0$" && grep -c "Tuning:" src/components/TabNotation.tsx | grep -q "^1$" && grep -c "STANDARD_TUNING" src/components/TabNotation.tsx | grep -q "^2$" && grep -c "KNOWN_TUNINGS" src/components/TabNotation.tsx | grep -q "^2$" && grep -c "Open E" src/components/TabNotation.tsx | grep -q "^1$" && grep -c "DADGAD" src/components/TabNotation.tsx | grep -q "^1$" && grep -q "^import { .*Text.* } from 'react-native'" src/components/TabNotation.tsx</automated>
  </verify>
  <done>TabNotation.tsx renders a compact "Tuning: <name>" label above the SVG grid when tab.tuning is non-standard; standard/null/undefined tuning renders no label; the KNOWN_TUNINGS lookup covers all 6 alt-tunings from the SYSTEM_PROMPT; Text is imported from react-native; `npx tsc --noEmit` reports zero errors for this file; core SVG rendering unchanged.</done>
</task>

</tasks>

<verification>
Post-implementation sanity checks (both must pass):

1. Server compiles: `python3 -m py_compile server/app/ai/breakdown.py`
2. Mobile type-checks: `cd mobile && npx tsc --noEmit` — zero new errors
3. Grep gates from `<automated>` blocks above all pass
4. Manual read of the SYSTEM_PROMPT change confirms the TUNING block reads as a natural extension of Fletcher's teaching voice (per fletcher-identity.md), not as a bolted-on instruction

No new tests are added. No EAS build is triggered from this plan. Live verification against Lenny/SRV (or another Open-E song) is manual and out of scope for the executor — it happens after the orchestrator ships and rebuilds.
</verification>

<success_criteria>
- server/app/ai/breakdown.py SYSTEM_PROMPT contains a TUNING (CRITICAL) section listing all 6 alt-tunings and mandating both tab.tuning emission + a "Tuning: <name>" technique note when non-standard
- server/app/ai/breakdown.py _TOOL_DEF description no longer contains "standard tuning unless the song calls for otherwise"
- mobile/src/components/TabNotation.tsx renders a "Tuning: <name>" label above the tab grid when tuning != standard EADGBE
- mobile/src/components/TabNotation.tsx KNOWN_TUNINGS lookup covers Open E, Open D, Open G, DADGAD, Drop D, Drop C — arrays match the SYSTEM_PROMPT exactly
- Defensive fallback: no label rendered when tuning is null/undefined/standard
- python3 -m py_compile passes for the server file
- npx tsc --noEmit passes for the mobile file
- No changes to Tab schema, Breakdown model, @governed decorator, or TabNotation's SVG core rendering
- Both files changed under a single atomic commit shape: `fix(04): tuning-aware Sonnet + mobile tuning label`
</success_criteria>

<output>
Create `.planning/quick/260909-01-sonnet-tuning-awareness/260909-01-SUMMARY.md` when done, listing:
- SYSTEM_PROMPT before/after byte counts (rough sanity signal that the TUNING block landed)
- _TOOL_DEF description change (one-line diff summary)
- TabNotation.tsx additions (new constants, new style keys, LOC delta)
- Verification command outputs (py_compile + tsc)
- Commit SHA of the single atomic commit
</output>
