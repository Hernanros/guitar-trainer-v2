---
phase: 04-cost-governor-node-verification
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - server/app/api/v1/song_of_day.py
  - .planning/RUNBOOK.md
  - mobile/src/api/generated/schema.d.ts
  - mobile/src/api/todaySong.ts
  - mobile/src/utils/quota.ts
  - mobile/src/components/SongOfDayCard.tsx
  - mobile/src/components/BreakdownErrorCard.tsx
  - mobile/src/app/breakdown/[songId].tsx
  - mobile/src/app/(tabs)/index.tsx
  - server/tests/test_song_of_day_quota.py
autonomous: false  # includes checkpoint:human-verify for RUNBOOK Console-cap manual step
requirements: [COST-03, COST-04]

must_haves:
  truths:
    - "GET /api/v1/song-of-day response body includes breakdown_quota: {remaining, cap, resets_at} for every request (D-06, SC-3)"
    - "SongOfDayCard renders the inline chip 'N left this week' below the CTA when today.breakdown_quota.remaining >= 1 (D-05)"
    - "When breakdown_quota.remaining === 0, the 'See the breakdown' CTA is disabled and its label reads 'Come back in {N} days' derived client-side from resets_at (D-02 + D-05)"
    - "BreakdownErrorCard renders Fletcher-voiced BREAKDOWN_CAPPED variant (heading 'Not my tempo.', no Try again button) when the breakdown route receives HTTP 429 with code='BREAKDOWN_CAPPED' (D-02)"
    - "BreakdownErrorCard renders Fletcher-voiced FLETCHER_OUT variant (heading 'Fletcher's on a break.', keeps Try again) when the breakdown route receives HTTP 503 with code='FLETCHER_OUT' (D-08)"
    - ".planning/RUNBOOK.md exists and documents the Anthropic Console $20/mo hard cap + 50/80/100% billing alert setup + FLETCHER_ADMIN_TOKEN generation (D-07, SC-4)"
    - "Human developer has confirmed via checkpoint that $20/mo Anthropic Console cap is set with billing alerts wired to hernan.rosenblum89@gmail.com (SC-4)"
  artifacts:
    - path: "mobile/src/utils/quota.ts"
      provides: "daysUntilReset(resets_at) helper used by SongOfDayCard and BreakdownErrorCard"
      exports: ["daysUntilReset"]
    - path: ".planning/RUNBOOK.md"
      provides: "Console cap + admin token generation runbook (D-07 + D-Claude admin-token)"
      contains: "Anthropic Console"
    - path: "server/app/api/v1/song_of_day.py"
      provides: "TodaySongResponse.breakdown_quota populated from governor_calls COUNT (D-06)"
      contains: "breakdown_quota"
  key_links:
    - from: "server/app/api/v1/song_of_day.py::get_song_of_day"
      to: "governor_calls table"
      via: "SELECT COUNT(*) FROM governor_calls WHERE user_id=:u AND feature='breakdown' AND created_at > now() - interval '7 days'"
      pattern: "FROM governor_calls"
    - from: "mobile/src/components/SongOfDayCard.tsx"
      to: "TodaySongResponse.breakdown_quota"
      via: "prop drilled from index.tsx: today.breakdown_quota"
      pattern: "breakdown_quota"
    - from: "mobile/src/app/breakdown/[songId].tsx"
      to: "mobile/src/components/BreakdownErrorCard.tsx"
      via: "code + resets_at props parsed from HTTP 429/503 error body"
      pattern: "code=.BREAKDOWN_CAPPED|FLETCHER_OUT"
---

<objective>
Slice B — Quota UI + Console cap wiring + Fletcher-voiced error cards.

Ship the mobile client's remaining-quota chip on the SongOfDayCard (COST-03), the extended BreakdownErrorCard that renders the BREAKDOWN_CAPPED and FLETCHER_OUT variants (D-02, D-08), the RUNBOOK entry for the $20/mo Anthropic Console cap (COST-04), and the server-side `breakdown_quota` field on TodaySongResponse (D-06).

**Working end-to-end at completion:** developer opens the mobile Today tab, sees "3 left this week" chip; taps See the breakdown 3 times; on 4th open of the same day, chip reads "0 left this week" (or the CTA is disabled with "Come back in 7 days"); tapping the breakdown route while capped shows the "Not my tempo." error card without a Try again button.

Purpose: give the user (COST-03) live visibility into their per-user cap AND give the developer (COST-04) an org-level $20/mo Anthropic-side hard stop via the Console.

Output: server-side TodaySongResponse.breakdown_quota population, mobile chip + disabled-CTA state on SongOfDayCard, extended BreakdownErrorCard with code-based switch, RUNBOOK.md, human checkpoint confirming the Console cap is manually set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-governor-node-verification/04-CONTEXT.md
@.planning/phases/04-cost-governor-node-verification/04-PATTERNS.md
@.planning/phases/04-cost-governor-node-verification/04-01-SUMMARY.md
@.planning/design/fletcher-identity.md
@server/app/api/v1/song_of_day.py
@server/app/api/v1/breakdowns.py
@server/app/models/song.py
@mobile/src/api/todaySong.ts
@mobile/src/components/SongOfDayCard.tsx
@mobile/src/components/BreakdownErrorCard.tsx
@mobile/src/app/(tabs)/index.tsx
@mobile/CLAUDE.md
@mobile/AGENTS.md

<interfaces>
Consumed from Slice A (04-01):
- `BreakdownQuota` Pydantic model (server/app/models/song.py) — already defined by 04-01
- `TodaySongResponse.breakdown_quota` field (server/app/models/song.py) — declared by 04-01; THIS PLAN populates it
- `governor_calls` table with `ix_governor_calls_user_feature_created` index (from 04-01 Alembic 0004)
- HTTP 429 body `{code:'BREAKDOWN_CAPPED', message, resets_at}` from breakdowns endpoint (04-01)
- HTTP 503 body `{code:'FLETCHER_OUT', message, retry_after_hint}` from breakdowns endpoint (04-01)

Cache-key invalidation note: SongOfDayCard's chip refreshes ONLY when TodaySongResponse refetches. Since the breakdown request lives on a separate route (/breakdown/[songId]), after a successful breakdown call the Today tab should invalidate `['today-song', userId, localCalendarDay()]` so the chip decrements on next Today-tab open. useSubmitRating already invalidates skill-graph — add the today-song key alongside it (this file lives in mobile/src/api/sessions.ts BUT sessions.ts is NOT in files_modified since we do not want to touch Slice C's rating logic — instead: use TanStack Query onSuccess in the breakdown route itself to invalidate today-song).

The breakdown route file is `mobile/src/app/breakdown/[songId].tsx` (exists per Phase 3 SUMMARY — not yet read; see read_first). It fetches the breakdown; on success it invalidates the today-song key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — populate TodaySongResponse.breakdown_quota from governor_calls</name>
  <read_first>
    - server/app/api/v1/song_of_day.py (self — mirror the existing SELECT patterns via `text()` + `.mappings()` lines 46-63 and lines 103-113; add the COUNT query and BreakdownQuota construction just before the `return TodaySongResponse(...)` at line 126)
    - server/app/models/song.py (self — BreakdownQuota model + TodaySongResponse.breakdown_quota field defined by 04-01)
    - server/app/models/db.py (self — GovernorCall ORM class defined by 04-01; used only for indirect type reference, we query via raw text() for consistency with existing song_of_day.py pattern)
    - .planning/phases/04-cost-governor-node-verification/04-CONTEXT.md (D-06 breakdown_quota shape, D-01 rolling 7-day COUNT arithmetic — the same window governor uses)
    - .planning/phases/04-cost-governor-node-verification/04-PATTERNS.md (§`server/app/api/v1/song_of_day.py` lines 573-611 — full modification template)
    - server/tests/test_today_song_selector.py (analog for a new test_song_of_day_quota.py — same DB fixture, ASGITransport, and X-User-ID header pattern)
  </read_first>
  <action>
    Modify `server/app/api/v1/song_of_day.py::get_song_of_day` per PATTERNS.md lines 573-611:
    (a) After the existing rating-row query (line 113) and BEFORE the `return TodaySongResponse(...)` at line 126, add one COUNT query: `quota_count = await db.scalar(text("SELECT COUNT(*) FROM governor_calls WHERE user_id = :user_id AND feature = 'breakdown' AND created_at > now() - interval '7 days'"), {"user_id": str(user_id)})`. Use text() + named params — same pattern as the existing reroll_marker/rating_row queries lines 54-63 + 103-113.
    (b) Compute resets_at per D-Claude 429-body-shape: `resets_at = MIN(created_at) FROM (same predicate) + 7 days`. If quota_count == 0, resets_at is `now() + 7 days`. Implement as a second scalar query: `oldest_call = await db.scalar(text("SELECT MIN(created_at) FROM governor_calls WHERE user_id = :user_id AND feature = 'breakdown' AND created_at > now() - interval '7 days'"), {"user_id": str(user_id)})`. If oldest_call is None: `resets_at_dt = datetime.now(timezone.utc) + timedelta(days=7)` else `resets_at_dt = oldest_call + timedelta(days=7)`. `resets_at_iso = resets_at_dt.isoformat()`.
    (c) Import `BreakdownQuota` from `app.models.song` at the top of the file. Import `datetime, timezone, timedelta` from datetime module (only if not already; existing imports at top of file already include most; add what's missing).
    (d) Construct `breakdown_quota = BreakdownQuota(remaining=max(0, 3 - (quota_count or 0)), cap=3, resets_at=resets_at_iso)`.
    (e) Add `breakdown_quota=breakdown_quota` as a kwarg to BOTH `return TodaySongResponse(...)` calls (the fresh-pick path line 126 AND the reroll path line 217).
    (f) Apply the same pattern to the reroll_today_song endpoint — the response is also `TodaySongResponse` and MUST include breakdown_quota (else Pydantic validation fails since 04-01 declared it non-optional).

    Create `server/tests/test_song_of_day_quota.py` mirroring `test_today_song_selector.py` fixture pattern. Tests:
    - test_breakdown_quota_defaults_to_three_when_no_calls: fresh user → response.breakdown_quota == {remaining: 3, cap: 3, resets_at: any-ISO}
    - test_breakdown_quota_decrements_after_governor_row: manually INSERT 1 governor_calls row for user + feature='breakdown' + created_at=now() → response.breakdown_quota.remaining == 2
    - test_breakdown_quota_zero_when_capped: INSERT 3 governor_calls rows → remaining == 0
    - test_breakdown_quota_ignores_other_features: INSERT 5 rows with feature='onboarding' → breakdown_quota.remaining stays at 3
    - test_breakdown_quota_ignores_old_calls: INSERT 3 rows with created_at = now() - interval '8 days' → remaining == 3 (aged out)
    - test_breakdown_quota_resets_at_matches_oldest_plus_7d: INSERT 1 row 2 days ago → resets_at parses to approximately (that timestamp + 7 days) within 1s tolerance
    - test_reroll_endpoint_also_includes_breakdown_quota: POST /api/v1/today-song/reroll → response.breakdown_quota present and matches GET's shape
  </action>
  <verify>
    <automated>cd server && DATABASE_URL="${TEST_DATABASE_URL:-postgresql://gt:devpass@localhost:5433/guitar_trainer}" python -m pytest tests/test_song_of_day_quota.py -x -v</automated>
    Also:
    - `grep -c "breakdown_quota" server/app/api/v1/song_of_day.py` >= 3 (import + fresh-pick return + reroll return)
    - `grep -c "FROM governor_calls" server/app/api/v1/song_of_day.py` == 2 (COUNT + MIN queries)
    - `grep -c "from app.models.song import.*BreakdownQuota" server/app/api/v1/song_of_day.py` == 1
    - Full suite: `cd server && python -m pytest tests/ -x` (no Phase 3 regression)
    - Curl smoke: `curl -sf -H "X-User-ID: <existing-user-uuid>" -H "X-Timezone-Offset: 0" http://localhost:8000/api/v1/song-of-day | jq -e '.breakdown_quota.remaining >= 0 and .breakdown_quota.cap == 3 and (.breakdown_quota.resets_at | fromdateiso8601)'` prints `true` (run against a local server + DB fixture)
  </verify>
  <done>Every `TodaySongResponse` returned by GET /api/v1/song-of-day AND POST /api/v1/today-song/reroll includes `breakdown_quota` with correct remaining computed from governor_calls COUNT within the last 7 days. All 7 quota tests pass. No Phase 3 test regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile — quota chip + disabled CTA + extended error card + today-song invalidation on breakdown fetch</name>
  <read_first>
    - mobile/CLAUDE.md AND mobile/AGENTS.md (Expo pinned to docs.expo.dev/versions/v57.0.0/ — verify any RN API used matches v57)
    - mobile/src/api/todaySong.ts (self — TodaySongResponse type is auto-generated from OpenAPI schema; regeneration required after server task 1)
    - mobile/src/components/SongOfDayCard.tsx (self — props interface lines 16-22 gains breakdown_quota; footer render logic lines 55-84; StyleSheet lines 89-166)
    - mobile/src/components/BreakdownErrorCard.tsx (self — single-variant heading/body/CTA pattern lines 17-49; extend with code + resets_at props + variant switch per PATTERNS.md lines 864-906)
    - mobile/src/app/(tabs)/index.tsx (self — pass `breakdown_quota={today.breakdown_quota}` to SongOfDayCard line 88-96)
    - mobile/src/app/breakdown/[songId].tsx (self — this file already exists per Phase 3 03-02 SUMMARY; READ IT to find where useQuery for breakdown lives; add onError handler that parses HTTP 429 → set errorCode='BREAKDOWN_CAPPED' + resets_at OR HTTP 503 with FLETCHER_OUT → set errorCode='FLETCHER_OUT'; pass to BreakdownErrorCard as new props; add onSuccess handler that invalidates ['today-song', userId, localCalendarDay()] so the chip decrements on next Today-tab open)
    - .planning/phases/04-cost-governor-node-verification/04-CONTEXT.md (D-05 chip copy + placement, D-02 429 body shape, D-08 503 body shape, `<specifics>` block for exact strings)
    - .planning/phases/04-cost-governor-node-verification/04-PATTERNS.md (§`mobile/src/components/SongOfDayCard.tsx` lines 751-819 — chip + disabled CTA; §`mobile/src/components/BreakdownErrorCard.tsx` lines 834-907 — code switch + resets_at derivation; §Shared Patterns `daysUntilReset` lines 1022-1034)
    - .planning/design/fletcher-identity.md (voice: "Not my tempo" leads, "Fletcher's on a break" for org-level)
  </read_first>
  <action>
    (a) Regenerate mobile OpenAPI types after Task 1 lands: `cd mobile && npm run generate-types` (or whatever script the repo uses — check package.json). This produces the new `TodaySongResponse.breakdown_quota` and `BreakdownQuota` types in `mobile/src/api/generated/schema.d.ts` (or similar generated file). Verify the type surfaces with `grep -c "BreakdownQuota" mobile/src/api/generated/schema.d.ts` >= 1.

    (b) Create `mobile/src/utils/quota.ts` per PATTERNS.md lines 1022-1034: export `daysUntilReset(resets_at: string | null | undefined): number` that returns `Math.max(0, Math.ceil((Date.parse(resets_at) - Date.now()) / (1000*60*60*24)))`. Return 0 for null/undefined/invalid input.

    (c) Extend `mobile/src/api/todaySong.ts` per PATTERNS.md lines 743-745: add `export type BreakdownQuota = components['schemas']['BreakdownQuota'];` convenience re-export.

    (d) Modify `mobile/src/components/SongOfDayCard.tsx` per PATTERNS.md lines 751-819:
      - Extend `SongOfDayCardProps` interface (line 16-22) with `breakdown_quota?: BreakdownQuota | null` (import BreakdownQuota from '../api/todaySong').
      - Add `breakdown_quota` to the destructured props at line 24.
      - Inside the default variant footer (lines 60-84 currently) — replace the existing `onSeekBreakdown` Pressable block with the pattern from PATTERNS.md lines 774-793: CTA with conditional style + label + disabled prop when `breakdown_quota?.remaining === 0`. Import `daysUntilReset` from `../utils/quota`.
      - AFTER the CTA (before the reroll ghost), render the chip: `{breakdown_quota && breakdown_quota.remaining >= 1 && <Text style={styles.quotaChip}>{breakdown_quota.remaining} left this week</Text>}` per D-05.
      - Extend StyleSheet with `ctaDisabled: { backgroundColor: '#555', opacity: 0.6 }` and `quotaChip: { fontSize: 12, color: '#999', textAlign: 'center', marginTop: 6 }` per PATTERNS.md lines 807-818.
      - Do NOT touch the already-rated variant (line 56-58) — chip is default-variant-only per D-05.

    (e) Modify `mobile/src/app/(tabs)/index.tsx`: pass `breakdown_quota={today.breakdown_quota}` to SongOfDayCard (line 88-96). No other logic change.

    (f) Modify `mobile/src/components/BreakdownErrorCard.tsx` per PATTERNS.md lines 834-907:
      - Extend `BreakdownErrorCardProps` (line 17-22) with `code?: 'BREAKDOWN_CAPPED' | 'FLETCHER_OUT' | null` and `resets_at?: string | null`.
      - Add destructured code + resets_at at line 24.
      - Compute variant heading/body per D-02 (BREAKDOWN_CAPPED: "Not my tempo." + "You've had 3 breakdowns this week. Come back in ${daysUntilReset(resets_at)} days.") and D-08 (FLETCHER_OUT: "Fletcher's on a break." + "Try again in an hour."). Default (no code) keeps the current "Fletcher lost the thread." + connection-dropped copy verbatim (do NOT change existing copy).
      - When code === 'BREAKDOWN_CAPPED', do NOT render the Try again button (cap is not retryable per D-02 — the button IS the wrong affordance). Keep Try again for FLETCHER_OUT + default. Always keep the "Back to today's song" secondary link.
      - Import daysUntilReset from '../utils/quota'.
      - StyleSheet unchanged.

    (g) Read `mobile/src/app/breakdown/[songId].tsx` (do this as part of read_first, then modify). Add error parsing: when the breakdown fetch returns an HTTP error, parse the body — apiClient's error object shape typically has status + body properties. If status === 429 and body.detail.code === 'BREAKDOWN_CAPPED', set errorCode='BREAKDOWN_CAPPED', errorResetsAt=body.detail.resets_at. If status === 503 and body.detail.code === 'FLETCHER_OUT', set errorCode='FLETCHER_OUT'. Otherwise pass code=null (default variant). Pass `code={errorCode}` and `resets_at={errorResetsAt}` to `<BreakdownErrorCard>`.
      Add TanStack Query onSuccess handler on the breakdown query (or an `onSettled` fallback) that invalidates `['today-song', userId, localCalendarDay()]` so the chip decrements on next Today-tab open. Import useQueryClient + getOrCreateUserId + localCalendarDay from '../../api/todaySong'.

    (h) Verify Expo v57 compatibility: any Pressable/View/Text usage MUST be from `react-native` (not `expo-router` or v58+ replacements). Cross-check apiClient error handling against v57 — Text component, StyleSheet, Pressable are all v57-native.

    NO fenced code blocks in action prose above.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
    Also:
    - `grep -c "breakdown_quota" mobile/src/api/todaySong.ts` >= 1
    - `grep -c "BreakdownQuota" mobile/src/api/generated/schema.d.ts` >= 1 (regenerated OpenAPI types)
    - `grep -c "daysUntilReset" mobile/src/utils/quota.ts` == 1
    - `grep -c "breakdown_quota" mobile/src/components/SongOfDayCard.tsx` >= 2 (prop + usage)
    - `grep -c "left this week" mobile/src/components/SongOfDayCard.tsx` == 1
    - `grep -c "ctaDisabled" mobile/src/components/SongOfDayCard.tsx` >= 1
    - `grep -c "code ===" mobile/src/components/BreakdownErrorCard.tsx` >= 2 (BREAKDOWN_CAPPED + FLETCHER_OUT switches)
    - `grep -c "Not my tempo" mobile/src/components/BreakdownErrorCard.tsx` == 1
    - `grep -c "Fletcher's on a break" mobile/src/components/BreakdownErrorCard.tsx` == 1
    - `grep -c "Fletcher lost the thread" mobile/src/components/BreakdownErrorCard.tsx` == 1 (default variant preserved)
    - `grep -c "BREAKDOWN_CAPPED\|FLETCHER_OUT" mobile/src/app/breakdown/*.tsx` >= 2
    - `grep -c "invalidateQueries.*today-song" mobile/src/app/breakdown/*.tsx` >= 1 (chip refresh wiring)
    - `grep -c "breakdown_quota" mobile/src/app/\(tabs\)/index.tsx` == 1
    - Existing Phase 3 tests still pass: `cd mobile && npm test -- --testPathPattern='(RatingPills|AlreadyRatedCard|TabNotation|index)' --passWithNoTests` exits 0
  </verify>
  <done>
- `cd mobile && npx tsc --noEmit` passes with zero errors
- grep confirms `quotaChip` and `ctaDisabled` StyleSheet keys present in `SongOfDayCard.tsx` (chip + disabled-CTA styling wired at the type/lint layer even if not yet rendered on-device)
- BreakdownErrorCard `code` prop switch handles all 3 variants: existing default ("Fletcher lost the thread."), BREAKDOWN_CAPPED ("Not my tempo."), FLETCHER_OUT ("Fletcher's on a break."); grep-verified via the 3 heading strings all present in the file
- Breakdown route file grep-confirms it parses `BREAKDOWN_CAPPED` and `FLETCHER_OUT` from error bodies and that `invalidateQueries.*today-song` fires on success
- Existing Phase 3 Jest tests still pass (RatingPills, AlreadyRatedCard, TabNotation, index)
- Visual confirmation of chip rendering, CTA disable state, and error-card variants is DEFERRED to the next EAS batch (see MEMORY: `eas-batch-phase3-device-verify`). Batching Slice B's device-verify with Phase 3's pending EAS build preserves the scarce-build budget per user memory `project_eas_budget`.
  </done>
</task>

<task type="auto">
  <name>Task 3: RUNBOOK.md + startup log reminder</name>
  <read_first>
    - .planning/phases/04-cost-governor-node-verification/04-CONTEXT.md (D-07 Console cap + billing alerts, D-Claude admin-token generation with openssl rand -hex 32, `<specifics>` block for exact hex-32 value guidance)
    - server/app/main.py (self — existing on_startup hook lines 49-55; add one INFO log line reminding the reader to verify Console cap per D-07)
  </read_first>
  <action>
    Create `.planning/RUNBOOK.md` with these sections:
    - **Purpose**: Manual setup steps that Claude cannot automate for Phase 4 cost governance and admin auth.
    - **Section 1 — Anthropic Console $20/mo Hard Cap (COST-04, D-07)**:
      - Navigate to https://console.anthropic.com → Settings → Billing → Usage Limits
      - Set "Hard limit" = $20.00 for the current calendar month
      - Set 3 billing alert thresholds: 50% ($10), 80% ($16), 100% ($20)
      - Set alert email to `hernan.rosenblum89@gmail.com`
      - Note: This is a per-organization cap (the whole POC org). It stops the org from exceeding $20 regardless of any per-user cap the app enforces.
      - Note: This is Console UI-only. The Anthropic Admin API is beta and adds a credential surface for zero benefit at POC scale — do not automate.
      - Verification: Console displays "$20 hard limit" in Usage Limits section.
    - **Section 2 — FLETCHER_ADMIN_TOKEN generation (Slice C dep, D-11 + D-Claude admin-token)**:
      - Generate a token: `openssl rand -hex 32` (produces a 64-char hex string)
      - Add to Railway env: `railway variables set FLETCHER_ADMIN_TOKEN=<generated-value>` OR via Railway dashboard (Project → Variables)
      - Add to `.env.example` in the server directory: `FLETCHER_ADMIN_TOKEN=<generate-with-openssl-rand-hex-32>` (already appended by Slice C — this entry is a reminder not a step)
      - Use: browser extension (e.g., ModHeader) injects `X-Admin-Token: <token>` on requests to `/api/v1/admin/curator` so the developer's browser can view the curator queue.
    - **Section 3 — Verifying the cap post-deploy**:
      - After Railway deploys, the server prints "REMINDER: verify $20/mo Anthropic Console cap is set" on startup — check Railway logs for this line to confirm the deploy is aware.

    Modify `server/app/main.py::on_startup` (lines 49-55): after the existing `logger.info("Startup: seed check complete.")` line, add ONE additional line: `logger.info("REMINDER: verify $20/mo Anthropic Console cap is set at anthropic.com/console (COST-04, D-07). See .planning/RUNBOOK.md.")`. Do NOT add the scheduler registration here — Slice D owns that; this task only adds the log reminder.
  </action>
  <verify>
    - `test -f .planning/RUNBOOK.md`
    - `grep -c "Anthropic Console" .planning/RUNBOOK.md` >= 1
    - `grep -c "\$20" .planning/RUNBOOK.md` >= 1
    - `grep -c "hernan.rosenblum89@gmail.com" .planning/RUNBOOK.md` == 1
    - `grep -c "FLETCHER_ADMIN_TOKEN" .planning/RUNBOOK.md` >= 1
    - `grep -c "openssl rand -hex 32" .planning/RUNBOOK.md` == 1
    - `grep -c "REMINDER: verify.*Anthropic Console cap" server/app/main.py` == 1
    - `cd server && python -m pytest tests/ -x` (full suite still green)
  </verify>
  <done>RUNBOOK.md created with all three sections; startup log line added; no test regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — Anthropic Console $20/mo cap is set with billing alerts</name>
  <what-built>
    Slice A/B automated everything that can be automated. This step confirms the ONE manual Console setup step succeeded, satisfying SC-4 (Anthropic Console shows a $20/mo hard cap with billing alerts wired to the developer's email).
  </what-built>
  <how-to-verify>
    1. Open https://console.anthropic.com in a browser and log in.
    2. Navigate to Settings → Billing → Usage Limits.
    3. Confirm the "Hard limit" is set to $20.00 for the current calendar month.
    4. Confirm three billing alert thresholds exist: 50% ($10), 80% ($16), 100% ($20).
    5. Confirm the alert email address is `hernan.rosenblum89@gmail.com`.
    6. (Optional smoke) Send a test alert from the Console if the UI offers it — check inbox for delivery to hernan.rosenblum89@gmail.com.

    If the Console UI has changed and these controls have moved, follow the RUNBOOK.md steps or search the Console docs for "usage limits" / "hard cap".
  </how-to-verify>
  <resume-signal>Type "approved" once the $20 cap and 3 alert thresholds are confirmed live in Console. If you cannot access the Console right now, type "deferred" and record a manual TODO in STATE.md to complete before Slice C ships (Slice C's Sonnet verifier will consume budget on real onboarding runs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (breakdown_quota field) | Server-computed from server-side COUNT; client receives read-only snapshot |
| mobile → apiClient error parsing | Server-controlled HTTP body; apiClient parses JSON; mobile code trusts the `code` field value |
| developer browser → Console UI | Manual step outside app surface; Console handles its own auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | breakdown_quota response leaks per-user cap state | accept | Cap state is only ever computed for the caller's own user_id (sourced from `Depends(get_user_id)` — Phase 2 D-04 boundary). A caller cannot request another user's quota. Same trust posture as the rest of Phase 2/3. |
| T-04-02-02 | Tampering | client sends fake breakdown_quota back on write path | mitigate | breakdown_quota is a response field only — no endpoint accepts it as input. Server-authoritative per D-06. |
| T-04-02-03 | Spoofing | attacker forges HTTP 429 body on the client to hide the chip | accept | Client-side render is UX only; the server's actual cap enforcement (Slice A) is the enforcement point. A forged client can only lie to itself. |
| T-04-02-04 | Denial of Service | flooding /api/v1/song-of-day inflates COUNT query load | mitigate | ix_governor_calls_user_feature_created index makes each COUNT O(log n) per user; no scan. Existing route already fires per-Today-tab-open — new COUNT + MIN queries add negligible cost. |
| T-04-02-05 | Repudiation | user claims cap was wrongly applied | mitigate | governor_calls is the auditable ledger (T-04-01-03 in Slice A). Developer can query for the disputed user_id + feature and prove exact call times. |
| T-04-02-06 | Tampering | RUNBOOK.md accidentally committed with a real FLETCHER_ADMIN_TOKEN value | mitigate | RUNBOOK.md instructs `openssl rand -hex 32` at read-time; the token value is NEVER written into RUNBOOK.md. The real value lives in Railway env only. |
| T-04-02-07 | Information Disclosure | daysUntilReset() leaks server clock via resets_at ISO string | accept | server clock is already implicitly exposed by every timestamped response; no new leak. |
| T-04-02-08 | Elevation of Privilege | manual Console cap step is skipped/forgotten | mitigate | Startup log line (task 3) surfaces on every Railway deploy; RUNBOOK.md documents; task 4 human checkpoint blocks Slice C progression until confirmed. |
</threat_model>

<verification>
Slice B is complete when:
1. `cd server && python -m pytest tests/test_song_of_day_quota.py -x` exits 0
2. `cd mobile && npx tsc --noEmit` exits 0
3. `grep -c "breakdown_quota" server/app/api/v1/song_of_day.py` >= 3
4. `grep -c "breakdown_quota" mobile/src/api/generated/schema.d.ts` >= 1
5. `grep -c "left this week" mobile/src/components/SongOfDayCard.tsx` == 1
6. `grep -c "Not my tempo\|Fletcher's on a break" mobile/src/components/BreakdownErrorCard.tsx` >= 2
7. `test -f .planning/RUNBOOK.md`
8. `grep -c "REMINDER: verify.*Anthropic Console cap" server/app/main.py` == 1
9. Human confirms Console cap is set (checkpoint task 4)
10. Full server test suite: `cd server && python -m pytest tests/ -x`
11. Existing mobile tests: `cd mobile && npm test -- --passWithNoTests`

**EAS device-verify deferred per project constraint (eas-budget memory): batched with Phase 3 pending EAS build. Note in SUMMARY that device-visible chip verification is deferred until next EAS batch.**
</verification>

<success_criteria>
- COST-03 satisfied: mobile client surfaces breakdown quota inline on SongOfDayCard AND surfaces the capped state via BreakdownErrorCard variant
- COST-04 satisfied: Console cap set (human-verified) AND RUNBOOK documents the process for future reset
- SC-3 (client surfaces remaining quota): met via chip + disabled CTA
- SC-4 (Console shows $20/mo hard cap with alerts to developer email): met via task 4 human checkpoint
- Slice C unblocked: no shared file conflict (Slice C touches deps.py, onboarding.py, admin.py, main.py include_router lines; Slice B touched main.py's startup hook — separate lines)
</success_criteria>

<output>
Create `.planning/phases/04-cost-governor-node-verification/04-02-SUMMARY.md` on completion. Include:
- Console-cap human checkpoint result + timestamp of confirmation
- Screenshots or curl-style JSON evidence of `breakdown_quota` field on TodaySongResponse
- Grep counts for all Fletcher voice strings
- Note that EAS device-verify is deferred to the pending Phase 3 EAS batch (per user memory eas-budget)
</output>
