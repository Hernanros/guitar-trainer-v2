---
phase: 01-foundation-empty-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - mobile/src/components/TabNotation.tsx
  - mobile/src/components/ChordDiagram.tsx
  - mobile/app/(tabs)/index.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Today tab renders a chord diagram (SVG fretboard grid with finger dot positions) for E7 and A7 from the hardcoded song payload"
    - "The Today tab renders a tab notation staff (SVG horizontal lines with fret numbers at beat positions) from the hardcoded measures"
    - "ChordDiagram and TabNotation components accept the semantic JSON shape from D-03 (Chord and Tab types from generated schema.d.ts) with no runtime errors"
    - "The components render without crash when given the full Sweet Home Chicago payload including open (fret=0) and muted (fret=-1) string positions"
  artifacts:
    - path: "mobile/src/components/ChordDiagram.tsx"
      provides: "react-native-svg chord diagram — 6 strings x 4 frets grid with finger dots, chord name label, muted string markers"
      exports: ["ChordDiagram"]
    - path: "mobile/src/components/TabNotation.tsx"
      provides: "react-native-svg tab staff — 6 horizontal string lines with fret numbers placed at beat positions"
      exports: ["TabNotation"]
  key_links:
    - from: "mobile/app/(tabs)/index.tsx"
      to: "mobile/src/components/ChordDiagram.tsx"
      via: "song.breakdown.chords.map(chord => <ChordDiagram chord={chord} />)"
      pattern: "ChordDiagram"
    - from: "mobile/app/(tabs)/index.tsx"
      to: "mobile/src/components/TabNotation.tsx"
      via: "song.breakdown.tab passed as tab prop"
      pattern: "TabNotation"
    - from: "mobile/src/components/ChordDiagram.tsx"
      to: "mobile/src/api/generated/schema.d.ts"
      via: "import type { components } — Chord type"
      pattern: "components\\[.schemas.\\]\\[.Chord.\\]"
    - from: "mobile/src/components/TabNotation.tsx"
      to: "mobile/src/api/generated/schema.d.ts"
      via: "import type { components } — Tab type"
      pattern: "components\\[.schemas.\\]\\[.Tab.\\]"
---

<objective>
Three-tab shell polish + semantic SVG rendering: implement ChordDiagram and TabNotation react-native-svg components that accept the D-03 semantic JSON payload, then update the Today tab to render both components using real data from the fetched song.

This plan runs in parallel with Plan 03 (MMKV persistence). It does not touch server/ or any files Plan 03 modifies.

Purpose: Prove the music data model (D-03) renders correctly on-screen before deploying. Phase 3 will replace the hardcoded content but must not change the component interfaces.
Output: Today tab shows a real SVG chord diagram grid and tab staff for Sweet Home Chicago's E7/A7 chords and measure 1 tab, loaded from the live API response.
</objective>

<execution_context>
@/Users/hernanrosenblum/Documents/guitar-trainer-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hernanrosenblum/Documents/guitar-trainer-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hernanrosenblum/Documents/guitar-trainer-v2/.planning/phases/01-foundation-empty-loop/01-CONTEXT.md
@/Users/hernanrosenblum/Documents/guitar-trainer-v2/.planning/phases/01-foundation-empty-loop/01-RESEARCH.md
@/Users/hernanrosenblum/Documents/guitar-trainer-v2/.planning/phases/01-foundation-empty-loop/01-01-SUMMARY.md

<interfaces>
<!-- Types consumed by these components — from mobile/src/api/generated/schema.d.ts (generated in Plan 01) -->
<!-- Access via: import type { components } from '../api/generated/schema' -->
<!-- Chord = components['schemas']['Chord'] -->
<!-- Tab = components['schemas']['Tab'] -->
<!-- ChordPosition = components['schemas']['ChordPosition'] -->
<!-- Note = components['schemas']['Note'] -->

<!-- ChordPosition.fret: 0=open string, -1=muted, 1..N=fret number -->
<!-- Chord.base_fret: starting fret of the diagram window (for chords high on the neck) -->
<!-- Chord.barre_fret: optional full/partial barre fret number -->
<!-- Chord.positions: list of 6 ChordPosition entries (one per string, string 1=high e, 6=low E) -->

<!-- react-native-svg primitives to use: Svg, G, Line, Circle, Text, Rect -->
<!-- Import: import { Svg, G, Line, Circle, Text, Rect } from 'react-native-svg' -->
<!-- No third-party guitar SVG library — hand-roll per D-03 and RESEARCH.md Don't Hand-Roll section -->

<!-- ChordDiagram layout constants (from RESEARCH.md Pattern 9): -->
<!-- STRING_SPACING = 24, FRET_SPACING = 28, STRINGS = 6, FRETS_SHOWN = 4 -->
<!-- width = (STRINGS - 1) * STRING_SPACING + 40 -->
<!-- height = FRETS_SHOWN * FRET_SPACING + 60 -->
<!-- Finger dots: cx = (pos.string - 1) * STRING_SPACING, cy = (pos.fret - chord.base_fret) * FRET_SPACING + FRET_SPACING/2 -->
<!-- Open strings (fret=0): render small open circle above the nut -->
<!-- Muted strings (fret=-1): render X marker above the nut -->

<!-- TabNotation layout: -->
<!-- 6 horizontal string lines, equal vertical spacing -->
<!-- Beats placed at equal horizontal intervals across the measure -->
<!-- At each beat, render fret numbers vertically for each note in beat.notes -->
<!-- string 1 = top line (high e), string 6 = bottom line (low E) -->
<!-- Duration labels optional in Phase 1; render fret numbers only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ChordDiagram and TabNotation react-native-svg components</name>
  <files>
    mobile/src/components/ChordDiagram.tsx,
    mobile/src/components/TabNotation.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-empty-loop/01-RESEARCH.md (Pattern 9: ChordDiagram implementation; Don't Hand-Roll section explaining why hand-rolled is correct; Anti-Patterns: no third-party guitar library)
    .planning/phases/01-foundation-empty-loop/01-CONTEXT.md (D-03: semantic music JSON model — Tab structure, Chord structure)
  </read_first>
  <action>
    Create mobile/src/components/ChordDiagram.tsx. Import Svg, G, Line, Circle, Text from react-native-svg. Import Chord type from the generated schema. Accept props: { chord: Chord }.

    Layout constants: STRING_SPACING=24, FRET_SPACING=28, STRINGS=6, FRETS_SHOWN=4.
    SVG width = (STRINGS-1)*STRING_SPACING + 40, height = FRETS_SHOWN*FRET_SPACING + 60.
    Translate inner group by (20, 30) for padding.

    Render:
    1. Nut/fret lines: FRETS_SHOWN+1 horizontal lines. The 0th fret line (nut) uses strokeWidth=3; others use strokeWidth=1.
    2. String lines: 6 vertical lines spaced STRING_SPACING apart, running from y=0 to y=FRETS_SHOWN*FRET_SPACING.
    3. For positions where fret > 0: filled black Circle at cx=(pos.string-1)*STRING_SPACING, cy=(pos.fret - chord.base_fret)*FRET_SPACING + FRET_SPACING/2, r=10.
    4. For positions where fret === 0 (open): small open Circle (stroke=black, fill=none) above the nut at cy=-12.
    5. For positions where fret === -1 (muted): Text "x" above the nut at cy=-12 (or a short diagonal cross using two Lines).
    6. Chord name: Text at x=(STRINGS-1)*STRING_SPACING/2, y=-26, textAnchor="middle", fontSize=14, fontWeight="bold". Content: chord.name.
    7. If chord.base_fret > 1: render a small Text label showing base_fret number to the right of the grid.

    Create mobile/src/components/TabNotation.tsx. Import Svg, G, Line, Text from react-native-svg. Import Tab type from the generated schema. Accept props: { tab: Tab }.

    Layout constants: STRING_SPACING=20, BEAT_WIDTH=40, LEFT_MARGIN=30.
    Render the first measure only in Phase 1 (tab.measures[0]). This is sufficient to prove the rendering; Phase 3 will handle multi-measure scrolling.

    SVG width = LEFT_MARGIN + tab.measures[0].beats.length * BEAT_WIDTH + 20.
    SVG height = 5 * STRING_SPACING + 40.

    Render:
    1. Six horizontal string lines, evenly spaced STRING_SPACING apart from y=0 to y=5*STRING_SPACING. Label left side with string names from tab.tuning array in reverse order (high e at top = index 0 of reversed tuning list).
    2. For each beat in the measure, at x = LEFT_MARGIN + beatIndex * BEAT_WIDTH: for each note in beat.notes, render the fret number as Text at x=beat_x, y=(note.string-1)*STRING_SPACING. textAnchor="middle", fontSize=12. Background: add a small white Rect behind each fret number so it occludes the string line at that position.

    Both components must be pure — no side effects, no state, no API calls.
  </action>
  <verify>
    <automated>
      cd /Users/hernanrosenblum/Documents/guitar-trainer-v2/mobile && npx tsc --noEmit 2>&1 | grep -E "ChordDiagram|TabNotation" | wc -l | xargs -I{} test {} -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - mobile/src/components/ChordDiagram.tsx exists and exports ChordDiagram as a named export
    - mobile/src/components/TabNotation.tsx exists and exports TabNotation as a named export
    - Both files import their prop types from "../api/generated/schema" using type-only imports
    - ChordDiagram renders fret lines, string lines, and finger dots using react-native-svg primitives (Svg, Line, Circle, Text)
    - ChordDiagram handles fret=-1 (muted) and fret=0 (open) without crashing — confirmed by reading the component source
    - TabNotation renders six horizontal line elements and fret number Text elements
    - npx tsc --noEmit in mobile/ produces zero errors referencing ChordDiagram.tsx or TabNotation.tsx
  </acceptance_criteria>
  <done>ChordDiagram and TabNotation are type-safe react-native-svg components that accept the D-03 semantic JSON shape. They are pure and stateless — ready for the Today tab to consume them.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ChordDiagram and TabNotation into the Today tab screen</name>
  <files>
    mobile/app/(tabs)/index.tsx
  </files>
  <read_first>
    mobile/src/components/ChordDiagram.tsx (just created — understand the Chord prop shape)
    mobile/src/components/TabNotation.tsx (just created — understand the Tab prop shape)
    mobile/src/api/songOfDay.ts (useSongOfDay hook return shape)
  </read_first>
  <action>
    Update mobile/app/(tabs)/index.tsx. Import ChordDiagram from ../../src/components/ChordDiagram and TabNotation from ../../src/components/TabNotation. Import ScrollView, View, Text, StyleSheet from react-native.

    The Today tab now renders (when song data is available):
    1. Song header section: title (bold, fontSize 24), artist (fontSize 16, muted color), genre + " · " + bpm + " BPM" + " · " + key (small, muted), difficulty badge.
    2. Technique Notes section: heading "How to play it" (Section header). For each item in song.breakdown.technique_notes: render a card with the note.heading in bold and note.body in regular text.
    3. Tab notation section: heading "Tab" (Section header). Render <TabNotation tab={song.breakdown.tab} />.
    4. Chord diagrams section: heading "Chords" (Section header). Render song.breakdown.chords.map(chord => <ChordDiagram key={chord.name} chord={chord} />). Lay the chord diagrams out in a horizontal row using a ScrollView with horizontal={true}.

    Wrap the whole screen in a ScrollView (vertical) so tall content is scrollable.

    Keep the loading state ("Loading...") and error state ("Could not load today's song") from Plan 01.

    Use StyleSheet.create for all styling. Colors: background #1A1A1A (dark), text #F5F5F5 (near-white), accent #E07B39 (warm orange from tab layout). Section headers use muted color #999.

    No inline styles. No hardcoded song data in the component — all data comes from useSongOfDay().
  </action>
  <verify>
    <automated>
      cd /Users/hernanrosenblum/Documents/guitar-trainer-v2/mobile && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} test {} -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/(tabs)/index.tsx imports ChordDiagram and TabNotation from their respective component files
    - mobile/app/(tabs)/index.tsx maps over song.breakdown.chords to render one ChordDiagram per chord
    - mobile/app/(tabs)/index.tsx passes song.breakdown.tab to TabNotation
    - mobile/app/(tabs)/index.tsx maps over song.breakdown.technique_notes to render technique note cards
    - mobile/app/(tabs)/index.tsx is wrapped in a ScrollView
    - mobile/app/(tabs)/index.tsx uses StyleSheet.create (no inline style objects except as references to StyleSheet entries)
    - npx tsc --noEmit exits with code 0 — zero TypeScript errors
    - The placeholder "[Tab notation — Plan 02]" and "[Chord diagram — Plan 02]" text from Plan 01 are removed
  </acceptance_criteria>
  <done>Today tab renders the full song payload: header, technique notes, tab staff, and chord diagrams — all from live API data via useSongOfDay, using type-safe react-native-svg components. Phase 3 will replace the dummy content without touching the component interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → SVG rendering | song.breakdown data from the API is mapped to SVG props; no user input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ChordDiagram / TabNotation receiving malformed data | accept | Components receive typed data from TanStack Query which validates against the generated schema. Malformed server responses surface as TypeScript type errors at compile time. Runtime guard: undefined checks on chord.positions before mapping prevent crash. |
| T-02-02 | Denial of Service | Very large breakdown payload causing SVG render freeze | accept | Phase 1 hardcoded payload is small (1 measure, 2 chords). TabNotation renders only measures[0]. No risk at Phase 1 scale. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — all react-native-svg dependencies were installed in Plan 01. Package audit already completed. |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript compiles: `cd mobile && npx tsc --noEmit` exits 0
2. ChordDiagram exported: `grep -c "export.*ChordDiagram" mobile/src/components/ChordDiagram.tsx` returns 1
3. TabNotation exported: `grep -c "export.*TabNotation" mobile/src/components/TabNotation.tsx` returns 1
4. Today tab wires both: `grep -c "ChordDiagram\|TabNotation" mobile/app/(tabs)/index.tsx` returns >= 2
5. No placeholder text remaining: `grep -c "Plan 02" mobile/app/(tabs)/index.tsx` returns 0
</verification>

<success_criteria>
- ChordDiagram and TabNotation are type-safe, stateless react-native-svg components accepting D-03 semantic types
- Today tab renders all breakdown sections: header, technique notes, tab staff, chord diagrams
- All data flows from useSongOfDay() — no hardcoded values in the UI layer
- TypeScript compiles without error
- Components handle open (fret=0) and muted (fret=-1) string positions without crashing
</success_criteria>

<output>
Create `.planning/phases/01-foundation-empty-loop/01-02-SUMMARY.md` when done.
</output>
