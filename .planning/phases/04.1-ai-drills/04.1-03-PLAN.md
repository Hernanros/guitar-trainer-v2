---
phase: 04.1-ai-drills
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - mobile/src/components/DrillCard.tsx
  - mobile/src/components/DrillCard.test.tsx
  - mobile/src/app/breakdown/[songId].tsx
  - mobile/src/api/breakdown.ts
  - mobile/src/api/generated/schema.d.ts
autonomous: true
requirements: [DRILL-02]

must_haves:
  truths:
    - "mobile/src/components/DrillCard.tsx renders a Fletcher-voiced drill card with name (title), 'For this song' or 'Any song' subtitle (based on song_specific), start_bpm → target_bpm tempo ladder, and a 'Start drill' Pressable that fires onStart(drillIndex)"
    - "mobile/src/app/breakdown/[songId].tsx renders a DRILLS section immediately BELOW the song header (which contains the label + title + artist + MetaLine) and immediately ABOVE the 'How to play it' (technique_notes) section (N2 clarification — 'at the top of the breakdown BODY, not the very top of the screen') when breakdown.drills is non-empty; the section is hidden entirely (not rendered as empty) when drills is [] or undefined"
    - "The DRILLS section uses the same section-title styling pattern as the existing 'How to play it' / 'Tab' / 'Chords' sections (uppercase, letterSpacing 1, color #999) — visual consistency"
    - "Tapping 'Start drill' on a DrillCard navigates via router.push(`/breakdown/${songId}/drill/${drillIndex}`) — Plan 04 creates the destination route; this plan just wires the navigation intent"
    - "The generated schema.d.ts includes the Drill type + BreakdownEnvelope wrapper (from Plan 02 B1 fix) so TypeScript enforces the shape end-to-end (no `any` in DrillCard.tsx). NOTE: Plan 02 changed the endpoint response shape from Breakdown → BreakdownEnvelope; useBreakdown consumers must be updated to read `data.breakdown.drills` instead of `data.drills`."
    - "useBreakdown hook is updated to align with the new envelope shape: return type becomes the BreakdownEnvelope (with `.breakdown` and `.drill_rated_today_indices` fields exposed to consumers). All callers of useBreakdown that touched `.tab`, `.chords`, `.technique_notes`, `.drills` directly on data must be updated to go through `.breakdown` (breaking change for breakdown/[songId].tsx — fixed in Task 3 of this plan)."
    - "DrillCard is tested at the render layer via compile-time assertions (matching mobile/__tests__/app/breakdown/[songId].test.tsx pattern — jest runtime not installed): name+subtitle+tempo-ladder+Start-button visible; onStart fires with the correct drillIndex when Start is tapped"
    - "Pre-4.1 backward compat: a BreakdownEnvelope where envelope.breakdown.drills=[] or drills missing renders the existing breakdown screen unchanged (no DRILLS section, no visual regression)"
  artifacts:
    - path: "mobile/src/components/DrillCard.tsx"
      provides: "Drill card render component + Pressable Start button"
      contains: "export function DrillCard"
    - path: "mobile/src/components/DrillCard.test.tsx"
      provides: "Compile-time contract tests for DrillCard (matching existing test-framework pattern)"
      contains: "describe('DrillCard'"
    - path: "mobile/src/app/breakdown/[songId].tsx"
      provides: "DRILLS section conditionally rendered immediately below song header, above technique_notes"
      contains: "DRILLS"
    - path: "mobile/src/api/breakdown.ts"
      provides: "useBreakdown hook updated to return BreakdownEnvelope shape (data.breakdown.* + data.drill_rated_today_indices)"
      contains: "BreakdownEnvelope"
    - path: "mobile/src/api/generated/schema.d.ts"
      provides: "Regenerated schema with Drill + Breakdown.drills + BreakdownEnvelope wrapper"
      contains: "BreakdownEnvelope"
  key_links:
    - from: "mobile/src/app/breakdown/[songId].tsx"
      to: "mobile/src/components/DrillCard.tsx"
      via: "envelope.breakdown.drills?.map((drill, i) => <DrillCard drill={drill} onStart={() => router.push(...)} />)"
      pattern: "DrillCard"
    - from: "mobile/src/components/DrillCard.tsx"
      to: "mobile/src/api/generated/schema.d.ts"
      via: "type Drill = components['schemas']['Drill']"
      pattern: "components\\['schemas'\\]\\['Drill'\\]"
    - from: "mobile/src/app/breakdown/[songId].tsx (DrillCard onStart)"
      to: "mobile/src/app/breakdown/[songId]/drill/[drillIndex].tsx (Plan 04)"
      via: "router.push(`/breakdown/${songId}/drill/${drillIndex}`)"
      pattern: "/drill/\\$\\{drillIndex\\}"
    - from: "mobile/src/api/breakdown.ts::useBreakdown"
      to: "server GET /api/v1/songs/{id}/breakdown (BreakdownEnvelope from Plan 02)"
      via: "response type is BreakdownEnvelope = { breakdown: Breakdown, drill_rated_today_indices: number[] }"
      pattern: "BreakdownEnvelope"
---

<objective>
Slice 3 — Mobile drill list on the breakdown screen + envelope-shape adaptation.

Insert a DRILLS section immediately below the song header on the breakdown detail screen (N2 clarification: "at the top of the breakdown BODY, not the very top of the screen"). Each drill renders as a Fletcher-voiced card with name, "For this song" / "Any song" subtitle, tempo ladder (start_bpm → target_bpm), and a "Start drill" button. Tapping Start navigates to a nested route (created in Plan 04). Backward-compatible: pre-4.1 cached breakdowns with no drills render the existing screen unchanged.

Also adapt useBreakdown + the breakdown screen to the new BreakdownEnvelope response shape from Plan 02 (B1 fix). The endpoint response changed from `Breakdown` to `{ breakdown: Breakdown, drill_rated_today_indices: number[] }` — mobile consumers must go through `.breakdown` to reach `.tab`, `.chords`, `.technique_notes`, `.drills`.

**Working end-to-end at completion:** With a locally-mocked breakdown envelope response containing 3 drills, the breakdown screen renders a DRILLS section immediately below the song header, each drill as a card, Start-button taps route to `/breakdown/{songId}/drill/{index}` (which is 404 until Plan 04 lands — expected). `tsc --noEmit` passes with no `any` in DrillCard.tsx. All existing breakdown-screen rendering (tab, chords, technique_notes) still works — just accessed via `data.breakdown.*` now.

Purpose: this is the mobile half of DRILL-02. It runs in Wave 2 in parallel with Plan 02 because it depends only on Plan 01's server Drill schema. HOWEVER, this plan must wait for Plan 02 to LAND before regen'ing schema.d.ts (because the envelope shape comes from Plan 02) — the schema-regen step is functionally Wave 3, handled by a explicit note in Task 1 that says "regen AFTER Plan 02 has been deployed to Railway." B4 fix: Plan 04 Task 1 also does a schema-regen sanity check to guard against the ordering hazard.

Output: DrillCard component (+ tests), regenerated schema.d.ts, useBreakdown updated for envelope shape, and the DRILLS section insertion in breakdown/[songId].tsx with the correct conditional-hide behavior. NOT included in this plan: the focused drill screen (Plan 04), the rating mutation (Plan 04), or the drill_rated_today_indices consumer logic on the parent screen (Plan 04 Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@mobile/AGENTS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-ai-drills/04.1-CONTEXT.md
@.planning/phases/04.1-ai-drills/04.1-RESEARCH.md
@.planning/phases/04.1-ai-drills/04.1-01-PLAN.md
@.planning/phases/04.1-ai-drills/04.1-02-PLAN.md
@.planning/design/fletcher-identity.md
@mobile/src/app/breakdown/[songId].tsx
@mobile/src/api/breakdown.ts
@mobile/src/components/RatingPills.tsx
@mobile/src/components/TabNotation.tsx
@mobile/__tests__/app/breakdown/[songId].test.tsx

<interfaces>
<!-- Contracts the executor must respect. -->

<!-- IMPORTANT: Expo has changed. Before writing any RN/Expo code, read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ per mobile/AGENTS.md. -->

Existing breakdown screen structure (mobile/src/app/breakdown/[songId].tsx):
- Loads `today` via useTodaySong()
- Loads `breakdown` via useBreakdown(songId)
- Renders (in order): song header (label+title+artist+MetaLine), 'How to play it' (technique_notes), 'Tab' (TabNotation), 'Chords' (ChordDiagram list), RatingPills / rated line
- N2 CLARIFICATION: The DRILLS section MUST go IMMEDIATELY BELOW the song header View (which ends after the MetaLine component) and IMMEDIATELY ABOVE the "Technique Notes" section (the View with sectionTitle "How to play it"). This positions drills at the top of the BREAKDOWN BODY (per CONTEXT.md — drills lead the practice unit) but NOT at the very top of the screen (the song header stays on top for context).

Expo Router pattern (already in use — see mobile/src/app/breakdown/_layout.tsx):
- Dynamic route: `[songId].tsx` → param `songId` string
- Nested: create `mobile/src/app/breakdown/[songId]/drill/[drillIndex].tsx` in Plan 04
- Navigation: `router.push(`/breakdown/${songId}/drill/${drillIndex}`)`
- Verify against https://docs.expo.dev/versions/v57.0.0/router/ before writing.

Drill type shape (from Plan 01, server/app/models/song.py::Drill):
```typescript
type Drill = {
  name: string;
  target_skill_temp_id: string;
  song_specific: boolean;
  what: string;
  tab_snippet: Tab;   // reuse existing Tab type
  start_bpm: number;
  target_bpm: number;
  repetitions: number;
  success_criterion: string;
  common_trap?: string | null;
};
type Breakdown = { tab: Tab; chords: Chord[]; technique_notes: TechniqueNote[]; drills: Drill[] };

// B1 FIX from Plan 02 — endpoint now returns this envelope:
type BreakdownEnvelope = {
  breakdown: Breakdown;
  drill_rated_today_indices: number[];   // 0-based drill indices rated for (user, song, today)
};
```

Existing test-framework convention (from mobile/__tests__/app/breakdown/[songId].test.tsx):
- @testing-library/react-native is NOT installed; tests are compile-time / pure-logic assertions.
- Tests live in mobile/__tests__/ (mirror the source path) so Expo Router doesn't auto-discover them as routes.
- Pattern: describe('Component'), it('behavior'), assertions on config/type shape/pure-logic transforms — NOT render assertions.
- DrillCard.test.tsx should follow this same pattern: assertions on the drill-shape typing, subtitle-string helpers, tempo-ladder formatter helpers, and prop-contract shape.

Styling tokens to reuse from breakdown/[songId].tsx:
- Section title: fontSize: 12, fontWeight: '700', color: '#999', textTransform: 'uppercase', letterSpacing: 1
- Card background: #242424, borderRadius: 10, padding: 14, marginBottom: 10, borderLeftWidth: 3, borderLeftColor: '#E07B39' (matches techniqueCard pattern)
- Card title (drill.name): fontSize: 15, fontWeight: '700', color: '#F5F5F5'
- Subtitle / meta line: fontSize: 13, color: '#999'
- Primary accent color: #E07B39 (Fletcher orange)

Schema regeneration approach:
- Codegen from server OpenAPI: `cd mobile && npm run generate` (script exists per Phase 4 Slice B precedent) — OR manually edit schema.d.ts as documented in the Slice B SUMMARY (STATE.md line: "schema.d.ts manually updated (server not running locally for codegen)"). Manual edit is acceptable per project precedent.
- CRITICAL ORDERING (B4 fix): regen MUST run AFTER Plan 02 has landed the BreakdownEnvelope and SessionResponse changes to the OpenAPI schema. If Plan 03 codegens against a Plan 02-less server, the envelope shape will be missing. Plan 04 Task 1 (B4 fix) does a schema-regen sanity check to catch this — but ideally, this Plan 03 Task 1 waits for Plan 02 to commit before running.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate mobile/src/api/generated/schema.d.ts with Drill type + BreakdownEnvelope wrapper + drill fields on SessionCreate/SessionResponse</name>
  <files>mobile/src/api/generated/schema.d.ts</files>
  <action>
    Regenerate the schema types so TypeScript picks up the Drill Pydantic model from Plan 01 AND the BreakdownEnvelope wrapper + SessionCreate/SessionResponse drill fields from Plan 02.

    **CRITICAL ORDERING (B4 fix):** This regen MUST run AFTER Plan 02 has committed its server changes (BreakdownEnvelope, drill_index in SessionCreate/SessionResponse). If Plan 02 is not yet merged when this task runs, the envelope shape will be MISSING from the regen output and downstream mobile code will fail. Verify Plan 02 is landed before running this task. Plan 04 Task 1 will re-verify with a separate schema-regen sanity check (B4 fix).

    Two acceptable paths:

    Path A (preferred if the server can be started locally): `cd server && uvicorn app.main:app --port 8000 &` then `cd mobile && npm run generate` (or the equivalent openapi-typescript command already wired into package.json). Confirms both Plan 01 (Drill) and Plan 02 (BreakdownEnvelope + SessionCreate.drill_index + SessionResponse.drill_index) landed.

    Path B (fallback, per Phase 4 Slice B precedent): manually edit mobile/src/api/generated/schema.d.ts. Add:
    1. A `Drill:` entry under `components.schemas` with the exact 10 properties (name, target_skill_temp_id, song_specific, what, tab_snippet ($ref to Tab), start_bpm, target_bpm, repetitions, success_criterion, common_trap) — types matching the Python types (string, string, boolean, string, Tab, integer, integer, integer, string, string|null).
    2. `drills: components['schemas']['Drill'][]` under `Breakdown.properties`.
    3. A `BreakdownEnvelope:` entry with properties `breakdown` ($ref to Breakdown) and `drill_rated_today_indices` (array of integer). This is the NEW endpoint response shape from Plan 02 B1 fix.
    4. Update the `GET /api/v1/songs/{id}/breakdown` response schema to reference BreakdownEnvelope instead of Breakdown.
    5. Add `drill_index` (integer, nullable) and `target_skill_node_id` (string, nullable, format=uuid) to both `SessionCreate.properties` and `SessionResponse.properties` (from Plan 02 Task 2).

    In either path, verify by running `cd mobile && npx tsc --noEmit` — no type errors.

    Path A is preferred because it guarantees fidelity with the Python model. If path A works, note it in SUMMARY.md; if you took path B, note that too.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; ! grep -q "error TS" /tmp/tsc.log && grep -q "Drill" mobile/src/api/generated/schema.d.ts && grep -q "BreakdownEnvelope" mobile/src/api/generated/schema.d.ts</automated>
  </verify>
  <done>schema.d.ts has Drill type, BreakdownEnvelope wrapper, and drill_index/target_skill_node_id on SessionCreate + SessionResponse. `tsc --noEmit` passes with 0 errors (W1 fix: shell semantics corrected to `! grep -q "error TS"` instead of the fragile `grep -c "error TS" | grep -qx 0` pattern). `grep -q "Drill"` and `grep -q "BreakdownEnvelope"` both succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DrillCard component with compile-time contract tests</name>
  <files>mobile/src/components/DrillCard.tsx, mobile/src/components/DrillCard.test.tsx</files>
  <behavior>
    Following the compile-time-contract pattern from mobile/__tests__/app/breakdown/[songId].test.tsx (no @testing-library/react-native runtime):

    - drill_type_shape: assert Drill type has all 10 fields with correct TypeScript shape (compile-time — a helper `const _drillTypeCheck: Drill = { ... }` with all fields would fail tsc if a field was missing).
    - subtitle_for_song_specific_true_returns_for_this_song: pure-fn `formatSubtitle(drill: Drill)` extracted from DrillCard returns "For this song" when drill.song_specific === true.
    - subtitle_for_song_specific_false_returns_any_song: same helper returns "Any song" when drill.song_specific === false.
    - tempo_ladder_formatter: pure-fn `formatTempoLadder(drill: Drill)` returns "60 → 70 BPM" for start_bpm=60, target_bpm=70.
    - card_accessibility_label: pure-fn `formatStartAccessibilityLabel(drill: Drill)` returns "Start drill: Isolate the b3→3 slide" for drill.name="Isolate the b3→3 slide".
    - onStart_prop_contract: `type DrillCardProps` has an onStart field typed as `(drillIndex: number) => void` (compile-time assertion via `const _propCheck: DrillCardProps = { drill, drillIndex: 0, onStart: (i) => { const _n: number = i; } }`).
    - common_trap_conditional_render_helper: pure-fn `shouldShowCommonTrap(drill: Drill)` returns true iff drill.common_trap is a non-empty string.

    These are structural / pure-fn tests, not render tests. The eight visual behaviors from the pre-revision plan (render_shows_name, render_shows_start_button, etc.) are covered by the on-device verification checkpoint in Plan 05.
  </behavior>
  <action>
    Create mobile/src/components/DrillCard.tsx. Consult https://docs.expo.dev/versions/v57.0.0/ before writing any RN component code (per mobile/AGENTS.md — Expo v57 API surface).

    Extract pure-fn helpers (for testing without a render framework):
    ```typescript
    export function formatSubtitle(drill: Drill): string {
      return drill.song_specific ? "For this song" : "Any song";
    }
    export function formatTempoLadder(drill: Drill): string {
      return `${drill.start_bpm} → ${drill.target_bpm} BPM`;
    }
    export function formatStartAccessibilityLabel(drill: Drill): string {
      return `Start drill: ${drill.name}`;
    }
    export function shouldShowCommonTrap(drill: Drill): boolean {
      return typeof drill.common_trap === 'string' && drill.common_trap.length > 0;
    }
    ```

    Props:
    ```typescript
    import type { components } from '../api/generated/schema';
    type Drill = components['schemas']['Drill'];

    interface DrillCardProps {
      drill: Drill;
      drillIndex: number;
      onStart: (drillIndex: number) => void;
    }

    export function DrillCard({ drill, drillIndex, onStart }: DrillCardProps) { ... }
    ```

    Layout (from top to bottom inside the card):
    - Row 1: drill.name (title style — fontSize 15, fontWeight '700', color '#F5F5F5')
    - Row 2: subtitle chips row — formatSubtitle(drill) (small text, color '#999'), separated by " · " from formatTempoLadder(drill)
    - Row 3 (body): drill.what (fontSize 14, color '#AAA', lineHeight 21)
    - Row 4 (success criterion): "Unlocked: {drill.success_criterion}" (fontSize 12, color '#8FBF6A' or Fletcher-orange fallback)
    - Row 5 (common_trap, conditional per shouldShowCommonTrap): "Watch for: {drill.common_trap}" (fontSize 12, color '#999', italic)
    - Row 6 (Start button): full-width Pressable with accessibilityLabel=formatStartAccessibilityLabel(drill), backgroundColor '#E07B39', borderRadius 8, height 44 (min tap target), text "Start drill" (color '#1A1A1A', fontWeight '700')

    Card container: backgroundColor '#242424', borderRadius 10, padding 14, marginBottom 10, borderLeftWidth 3, borderLeftColor '#E07B39'.

    Do NOT include tab_snippet render in the card — lives in Plan 04's focused drill screen.
    Do NOT wrap in an outer ScrollView — parent provides one.

    Fletcher voice sanity check (per fletcher-identity.md): "For this song" / "Any song" / "Unlocked: ..." / "Watch for: ..." / "Start drill" — all sharp, no exclamation points. Do NOT add "Let's go!" or similar.

    Create mobile/src/components/DrillCard.test.tsx following the compile-time-contract pattern from mobile/__tests__/app/breakdown/[songId].test.tsx. Seven tests as listed in <behavior>. Include a `makeDrill(overrides?)` helper at the top for fixture creation. No @testing-library/react-native imports.
  </action>
  <verify>
    <automated>cd mobile && npx jest DrillCard --no-coverage 2>&1 || cd mobile && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; ! grep -q "error TS" /tmp/tsc.log</automated>
  </verify>
  <done>DrillCard exports pure-fn helpers + component. All 7 tests pass (or, if jest runtime is unavailable per the existing convention, tsc --noEmit passes and confirms the compile-time contract assertions in DrillCard.test.tsx type-check successfully). tsc --noEmit is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update useBreakdown for envelope shape + insert DRILLS section into breakdown screen with conditional-hide + Start-button navigation</name>
  <files>mobile/src/api/breakdown.ts, mobile/src/app/breakdown/[songId].tsx</files>
  <action>
    STEP 1 — Update useBreakdown for the new envelope shape (from Plan 02 B1 fix):

    In mobile/src/api/breakdown.ts, change the useBreakdown return type from `Breakdown` to `BreakdownEnvelope`:
    ```typescript
    import type { components } from './generated/schema';
    type BreakdownEnvelope = components['schemas']['BreakdownEnvelope'];

    export function useBreakdown(songId: number | null) {
      return useQuery<BreakdownEnvelope>({
        queryKey: ['breakdown', songId],
        // ... existing config (staleTime: Infinity, retry: 0, gcTime, refetchOnWindowFocus: false) unchanged
        queryFn: async () => {
          const res = await apiFetch<BreakdownEnvelope>(`/api/v1/songs/${songId}/breakdown`);
          return res;
        },
      });
    }
    ```

    STEP 2 — Update mobile/src/app/breakdown/[songId].tsx to consume the envelope:

    Find every reference to `breakdown.data.tab`, `breakdown.data.chords`, `breakdown.data.technique_notes` and change them to `breakdown.data.breakdown.tab`, `breakdown.data.breakdown.chords`, `breakdown.data.breakdown.technique_notes`. Simplest pattern: introduce a local `const bd = breakdown.data?.breakdown;` alias (which was previously `breakdown.data`) so most call sites need only one edit at the top.

    Example diff:
    ```typescript
    // Before:
    const bd = breakdown.data;   // Breakdown
    // ... bd.tab, bd.chords, bd.technique_notes
    // After:
    const envelope = breakdown.data;   // BreakdownEnvelope
    const bd = envelope?.breakdown;    // Breakdown (unwrapped)
    // ... bd.tab, bd.chords, bd.technique_notes  (unchanged from here)
    ```

    STEP 3 — Insert the DRILLS section:

    N2 CLARIFICATION: insert IMMEDIATELY below the song header View (which ends after the MetaLine — locate by searching for the closing `</View>` right after the artist/label/title/MetaLine block) and IMMEDIATELY above the "Technique Notes" section (the View with sectionTitle "How to play it"). This is the top of the breakdown BODY, not the top of the screen.

    ```tsx
    {bd?.drills && bd.drills.length > 0 && (
      <View style={styles.section}>
        <Text style={styles.sectionTitle}>Drills</Text>
        {bd.drills.map((drill, i) => (
          <DrillCard
            key={`${drill.target_skill_temp_id}-${i}`}
            drill={drill}
            drillIndex={i}
            onStart={(idx) => router.push(`/breakdown/${songId}/drill/${idx}`)}
          />
        ))}
      </View>
    )}
    ```

    Add the import at the top: `import { DrillCard } from '../../components/DrillCard';`

    The key uses `target_skill_temp_id + index` for stable identity across an admin regen that shuffles drill order (RESEARCH.md landmine #7).

    Do NOT change 'How to play it' / 'Tab' / 'Chords' / RatingPills sections (aside from the `bd.*` → `bd.*` no-op after the envelope unwrap alias).

    Do NOT render an "empty state" for DRILLS — hidden entirely (Grace-B pattern) when drills is empty or undefined.

    Do NOT expose `envelope.drill_rated_today_indices` in this plan — that's consumed by Plan 04 Task 3 for the drill-primary UI state. This plan just makes the field ACCESSIBLE via the envelope typing.

    Consult https://docs.expo.dev/versions/v57.0.0/router/ before writing router.push.

    Sanity-check: `cd mobile && npx tsc --noEmit` clean. Existing behavior when `bd?.drills` is `[]` or missing MUST be visually identical to pre-plan state.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; ! grep -q "error TS" /tmp/tsc.log && grep -q "DrillCard" "mobile/src/app/breakdown/[songId].tsx" && grep -q "envelope\\|BreakdownEnvelope" mobile/src/api/breakdown.ts</automated>
  </verify>
  <done>useBreakdown returns BreakdownEnvelope. Breakdown screen unwraps envelope via alias and renders DRILLS section immediately below song header, above 'How to play it'. Hidden entirely when drills is empty or missing. Start button routes to `/breakdown/${songId}/drill/${index}` (route lands in Plan 04). tsc passes (W1 fix: shell semantics use `! grep -q` instead of `grep -c | grep -qx 0`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server GET /api/v1/songs/{id}/breakdown → mobile client | drill.name / drill.what / drill.common_trap are Sonnet-authored strings; XSS is not a concern in RN Text (no HTML rendering) |
| mobile navigation | songId and drillIndex are URL params; validated on server-side (Plan 02) not on client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-09 | Information disclosure | Drill copy leaks in shared screenshots | accept | Drills are user-owned content (same trust class as tab/chords already displayed); no PII in drill fields |
| T-04.1-10 | Tampering | User inspects and edits schema.d.ts to remove Drill/BreakdownEnvelope | accept | Client-side type theatre; server enforces all constraints (Plans 01+02) |
| T-04.1-SC | Tampering | No package installs in this plan | accept | Plan does not touch package.json or install new deps |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` — 0 errors.
- `cd mobile && npx jest DrillCard --no-coverage` — passes (or the compile-time-contract tests type-check successfully if jest runtime is not configured — matches existing convention).
- Manual smoke (dev build): pull latest, tap into a song's breakdown; if `envelope.breakdown.drills` is empty (pre-4.1 cache — likely for existing seed songs), the screen looks identical to before this plan. If drills exist, a DRILLS section appears immediately below the song header, above 'How to play it'. Tapping Start pushes to the nested route (which 404s until Plan 04).
- Backward compat: no visual change on any breakdown that lacks drills.
- Envelope-shape sanity: `grep -n "envelope\\.breakdown\\|data\\.breakdown\\." mobile/src/app/breakdown/[songId].tsx` should show every consumer going through `.breakdown` to reach `.tab`/`.chords`/`.technique_notes`/`.drills`.
</verification>

<success_criteria>
- DRILL-02 partially satisfied: DRILLS section renders immediately below the song header (top of breakdown body); drill cards show name + target skill (via subtitle) + tempo ladder + Start button.
- Full DRILL-02 completion depends on Plan 04 (focused drill screen).
- Zero regression on existing breakdown screen behavior when drills is empty/missing.
- Fletcher voice preserved in all component copy.
- Envelope adaptation complete: useBreakdown returns BreakdownEnvelope; breakdown screen unwraps via `bd = envelope.breakdown` alias; drill_rated_today_indices is available for Plan 04 consumption.
- W1 shell-semantics fix applied to all `tsc --noEmit` verify commands: `! grep -q "error TS"` instead of `grep -c "error TS" | grep -qx 0`.
- N2 clarification applied: DRILLS section positioning is "immediately below the song header, immediately above 'How to play it'" — documented in must_haves + Task 3 action.
</success_criteria>

<output>
Create `.planning/phases/04.1-ai-drills/04.1-03-SUMMARY.md` when done, listing:
- Files touched (mobile-side only in this plan)
- Codegen approach taken (path A or path B)
- Confirmation that Plan 02 was landed before this task ran (B4 ordering hazard)
- Deviations from the styling spec (if any — e.g., no green success-tint palette existed and you fell back to Fletcher orange)
- Screenshot placeholder note: "Device screenshot deferred to Plan 05 verification wave"
- Handoff notes for Plan 04 (which owns the nested drill/[drillIndex].tsx route AND consumes envelope.drill_rated_today_indices for the parent-screen disable logic)
</output>
